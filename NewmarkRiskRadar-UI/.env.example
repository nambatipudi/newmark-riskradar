# Base URL the browser uses. Left relative so requests go through the Next rewrite in next.config.ts
# and never hard-code the API host into the client bundle.
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Origin the Next server (and the rewrite) forwards to. Use http://api:8080 under Docker Compose.
API_PROXY_TARGET=http://localhost:5000
